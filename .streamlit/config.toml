[theme]
base = "light"

[client]
toolbarMode = "viewer"

[server]
headless = true          # recommended for cloud deployments

[runner]
magicEnabled = false     # disables "magic" auto-display of variables (cleaner behavior)
